version: '3.8'

services:
  fortress:
    build:
      context: .
      dockerfile: Dockerfile
    image: fortress:latest
    container_name: fortress-app
    ports:
      - "9002:3000"
    env_file:
      - .env
    restart: unless-stopped
